
## compile environment setup
To build optee driver for 1195, copy the project to linux kernel
	"cp optee_linuxdriver linux/drivers/optee -a"

go to kernel directory then type
	"M=drivers/optee make -e modules"







